{
  "buildCommand": "npm install --legacy-peer-deps && node scripts/setup-db.js && next build",
  "installCommand": "npm install --legacy-peer-deps",
  "framework": "nextjs",
  "outputDirectory": ".next"
}
